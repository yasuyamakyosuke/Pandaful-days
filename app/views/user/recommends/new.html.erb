<div class ="container">
    <div class ="row">
      <h3>下記の情報を記載し私達にお勧めのパン屋を教えてください！</h3><br>
      <h3>確認後掲載させて頂きます</h3>
    </div>
    <div class ="row">
      <table class ="table">
        <%= form_with(model:@recommend_new, url:recommends_path) do |form| %>
          <tr>
            <th><%= form.label :画像 %></th>
            <td><%= form.attachment_field :recommend_image %></td>
          </tr>
          <tr>
            <th><%= form.label :店名 %></th>
            <td><%= form.text_field :recommend_shop_name %></td>
          </tr>
          <tr>
            <th><%= form.label :オススメポイント %></th>
            <td><%= form.text_area :recommend_shop_profile %></td>
          </tr>
          <tr>
            <th><%= form.label :ジャンル %></th>
            <td><%= form.collection_select(:genre_id,Genre.all, :id, :genre_name) %></td>
          </tr>
          <tr>
            <th><%= form.label :駅 %></th>
            <td><%= form.collection_select(:station_id,Station.all, :id, :station_name) %></td>
          </tr>
          <tr>
            <th><%= form.label :店舗住所 %></th>
            <td><%= form.text_field :recommend_shop_address %></td>
          </tr>
          <tr>
            <th><%= form.label :ホームページ %></th>
            <td><%= form.text_field :recommend_shop_homepage %></td>
          </tr>
          <tr>
            <th><%= form.submit :送信する %></th>
          </tr>
        <% end %>
      </table>
      </div>
    </div>
  </div>
</div>